{% extends 'users.html.twig' %}

{% block title %} Modification du profil de  {{app.user.firstname}} {{app.user.name}}{% endblock %}

{% block body %}
    <div class="grix xs2 container">
        <div class="col-xs4"><h1> Profil de {{app.user.firstname}} {{app.user.name}}</h1></div>
       
        <div class="col-xs4">
        <h1> Modification du profil de  {{app.user.firstname}} {{app.user.name}}</h1>
        {{form(form)}}
           
     
    </div>
{% endblock %}
